<h1>Leaderboard</h1>
<table>
  <tr>
    <th>Player</th>
    <th>Wins</th>
    <th>Losses</th>
  </tr>

  <% @players.each do |player| %>
    <tr>
      <td><%= player.name %></td>
      <td><%= player.wins_count %></td>
      <td><%= player.losses_count %></td>
    </tr>
  <% end %>
</table>